# Entered code is

Compares the stored access code with the text supplied to the block.

```sig
a4MicroSystemAccess.enteredCodeIs("1234")
```

The block returns `true` only when every digit and the code length match. Clear the stored code after validation before starting a new entry.
