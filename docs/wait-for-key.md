# Wait for keypad key

Pauses the current program flow until a keypad character is received, then returns that character.

```sig
a4MicroSystemAccess.waitForKey()
```

Possible results are `0` to `9`, `*` and `#`.
