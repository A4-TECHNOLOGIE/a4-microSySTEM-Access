# Clear entered code

Removes all digits from the stored access code.

```sig
a4MicroSystemAccess.clearEnteredCode()
```

This does not discard keypad characters that are still waiting to be read by the `read keypad key` block.
