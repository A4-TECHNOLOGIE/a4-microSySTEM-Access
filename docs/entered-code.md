# Entered code

Returns the digits currently stored as the access code.

```sig
a4MicroSystemAccess.getEnteredCode()
```

Numeric keys are appended automatically, up to 16 digits. The `*` key clears the code. The `#` key is intended for validation and is not appended.
