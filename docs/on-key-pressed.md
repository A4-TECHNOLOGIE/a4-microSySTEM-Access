# On keypad key pressed

Runs the blocks inside the event whenever a valid keypad key is pressed.

```sig
a4MicroSystemAccess.onKeyPressed(function (key) {
})
```

The `key` parameter contains `0` to `9`, `*` or `#` and can be dragged into other blocks as a value.
