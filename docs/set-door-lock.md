# Set door lock

Sets the magnetic lock connected to C0 to the selected state.

```sig
a4MicroSystemAccess.setDoorLock(a4MicroSystemAccess.DoorLockState.Locked)
```

Select `locked` to energize the magnetic lock and keep the door closed. Select `unlocked` to switch off the magnetic lock and release the door.
