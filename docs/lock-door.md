# Lock door

Energizes the magnetic lock connected to C0 and keeps the door locked.

```sig
a4MicroSystemAccess.lockDoor()
```

The magnetic lock remains powered until the `unlock door` block is used or the model is switched off.
